[package]
name = "surrealism-color"
version = "0.1.0"
edition = "2024"
authors = ["Tobie Morgan Hitchcock <tobie@surrealdb.com>"]
description = "Color conversion, manipulation, and accessibility functions for Surrealism"
license = "MIT OR Apache-2.0"
repository = "https://github.com/surrealdb/surrealism"
publish = false

# Each module in this repo is an independent Cargo project (its own workspace,
# own Cargo.lock) so it can be built, versioned, and eventually published on
# its own.
[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]
surrealism = "0.5"
surrealdb-types = "3.2"

[lints.clippy]
allow_attributes = "allow"
